mutation UpdateDatabase($database: DatabaseInput!) {
  updateDatabase(input: {database: $database}) {
    database {
      id
      title
      createdAt
      updatedAt
      deletedAt
    }
    errors {
      path
      message
    }
  }
}
