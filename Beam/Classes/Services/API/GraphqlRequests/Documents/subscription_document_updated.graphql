subscription ($id: ID!) {
  documentUpdated(id: $id) {
    document {
      id
      title
      slug
      data
      createdAt
      updatedAt
      deletedAt
      isPublic
      database {
        title
        id
      }
      documentType
    }
  }
}
