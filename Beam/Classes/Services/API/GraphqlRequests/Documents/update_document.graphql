mutation UpdateDocument($title: String!, $id: ID, $data: String) {
  updateDocument(input: {document: {title: $title, id: $id, data: $data}}) {
    document {
      id
    }
    errors {
      path
      message
    }
  }
}
