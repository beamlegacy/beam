mutation UpdateDocuments($documents: [DocumentInput!]!) {
  updateDocuments(input: {documentsInput: $documents}) {
    documents {
      id
      title
      data
      database {
        id
        title
      }
    }
    errors {
      path
      message
    }
  }
}
