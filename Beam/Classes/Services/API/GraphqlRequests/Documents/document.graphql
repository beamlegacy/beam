query document($id: ID!) {
  document(id: $id) {
    id
    title
    slug
    data
    createdAt
    updatedAt
    deletedAt
    isPublic
    database { title id }
    documentType
    publicUrl
  }
}
