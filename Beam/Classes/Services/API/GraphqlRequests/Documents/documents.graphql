query ($updatedAtAfter: ISO8601DateTime) {
  me {
    documents(updatedAtAfter: $updatedAtAfter) {
      id
      checksum
      title
      data
      createdAt
      updatedAt
      deletedAt
      isPublic
      database { title id }
      documentType
    }
  }
}
