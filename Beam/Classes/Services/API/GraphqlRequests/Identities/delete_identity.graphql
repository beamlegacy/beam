mutation DeleteIdentity($identity: IdentityMinimalInput!) {
  deleteIdentity(input: {identity: $identity}) {
    identity {
      id
    }
    errors {
      objectid
      path
      message
    }
  }
}
