notarize:
  dependencies:
    - build:dmg
  stage: notarize
  artifacts:
    expire_in: 7d
    paths:
      - builds/
  only:
    - merge_requests
    - master
    - develop
  script:
    - bundle exec fastlane notarize_build
  tags:
    - macos
  interruptible: true
